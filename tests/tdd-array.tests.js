const assert = require('assert')
const arrayMethod = require('../lib/tdd-array.tests')

describe('tdd and array methods', () => {

  it('return array of squares greater than 20', () => {
	/*	[1, 4, 5, 7] | [25, 49] */

		const array = [1, 4, 5, 7, 9]
		arrayMethod.buildArrayOfSquares(array){
			console.log(array, endArray);
		}
/* 		arrayMethod.deepEquals(array, [25, 49, 81])
 */
	})
})
